<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.foodgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".Activities.OptionalSaveStatistics" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.HowToPlay" />
        <activity android:name=".Activities.Leaderboard" />
        <activity android:name=".Activities.Registration" />
        <activity android:name=".CatchingGame.CatchingActivity"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".MazeGame.MazeActivity"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".MonsterGame.MonsterActivity"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>